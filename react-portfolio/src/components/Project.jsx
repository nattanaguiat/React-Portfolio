function Project () {
    const { name, repo, link, description } = props;

    return (
        <div
    )
// Return the proyect componet for each individual proyect props name repo link description.

}

export default Project